/* 0003_omniscient_the_enforcers.sql
   NO-OP migration.

   Reason:
   This migration was generated during a snapshot/journal reset and contains
   CREATE TABLE statements for tables that already exist in the database.
   Running it causes ER_TABLE_EXISTS_ERROR.

   The database schema is already present (tables + constraints), and later
   migrations handle any incremental changes.
*/

SELECT 1;
